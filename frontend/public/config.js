// Default runtime config (dev / fallback). Auth is OFF when oidcAuthority is empty.
// In production this file is regenerated from container env at startup
// (see docker-entrypoint.d/40-render-config.sh) — do not put real values here.
window.__APP_CONFIG__ = {
  oidcAuthority: "",
  oidcClientId: "lab-tracker",
};

{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "group:allNonMajor"
  ],
  "assignees": ["Seklfreak"],
  "packageRules": [
    {
      "description": "Auto-merge non-major updates once the test workflow passes.",
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "automerge": true
    },
    {
      "description": "Major updates: open a PR but require manual review/merge.",
      "matchUpdateTypes": ["major"],
      "automerge": false
    },
    {
      "description": "Skip TypeScript 7.0.x: it ships no stable programmatic API, so typescript-eslint (peer typescript <6.1.0) can't run on it and the frontend install/lint breaks. The API is targeted for 7.1 — allow that through so a fresh PR reopens when it lands.",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<7.0.0 || >=7.1.0"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "SwiftLint and XcodeGen are downloaded from their GitHub releases in the workflow, so no built-in manager sees the pinned versions. Reads the `# renovate:` annotation above each env var, so a third pinned tool needs the annotation and nothing here.",
      "managerFilePatterns": [
        "/(^|/)\\.github/workflows/.*\\.ya?ml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>\\S+)\\s+[A-Z0-9_]+: \"(?<currentValue>[^\"]+)\""
      ],
      "versioningTemplate": "semver"
    }
  ]
}
